
- [x] Remove directories from version search without a studiopro.exe file.
- [x] Remove directories from project search without a .mpr file.
- [ ] In `convert` command, execute each matched project's conversion in parallel to ongoing ones.
- [ ] Implement ui for list and implement in `list` and `open` commands
- [ ] Implement ui where user can select 1 item from list and implement it in `open` command
- [ ] Implement ui where user can see all ongoing conversions in table with loading indication for `convert` command
- [ ] Implement ui where user can run the root command and get UI showing his config, projects and versions